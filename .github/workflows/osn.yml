name: EPG OSN.com AR 7 Days Update

on:
  schedule:
    - cron: "0 11 * * *"   # يوميًا الساعة 11:00 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  epg_osn_com_ar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Clone EPG repo
        run: |
          git clone --depth 1 https://github.com/iptv-org/epg.git
          cd epg
          npm install

      - name: Parse OSN channels (AR)
        run: |
          cd epg
          npm run channels:parse --- \
            --config=./sites/osn.com/osn.com.config.js \
            --output=./sites/osn.com/osn.com_ar.channels.xml \
            --set=lang:ar

      - name: Generate 7-day EPG (osn.com AR)
        run: |
          cd epg
          npm run grab --- \
            --channels=sites/osn.com/osn.com_ar.channels.xml \
            --days=7 \
            --lang=ar

      - name: Save XML
        run: |
          mkdir -p public
          cp epg/guide.xml public/epg-osn-com-ar-7days.xml

      - name: Compress XML (gzip)
        run: |
          gzip -c public/epg-osn-com-ar-7days.xml > public/epg-osn-com-ar-7days.xml.gz

      - name: Commit & Push
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add public/epg-osn-com-ar-7days.xml public/epg-osn-com-ar-7days.xml.gz
          git diff-index --quiet HEAD || git commit -m "Update 7-day EPG OSN.com AR"
          git pull --rebase origin main
          git push origin main
